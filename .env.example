# Root environment reference — copy workspace-specific files instead:
#
#   cp backend/.env.example backend/.env
#   cp frontend/.env.example frontend/.env.local
#
# Then start Docker and the apps:
#
#   npm run docker:up
#   npm install
#   npm run db:generate
#   npm run db:migrate
#   npm run dev
